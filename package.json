{
  "name": "simulador-ir",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "docker-compose up"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
